# torch / torchvision are installed via the CUDA wheels in the Dockerfile.
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
ultralytics>=8.3
pillow>=10.0
numpy>=1.26
opencv-python-headless>=4.9
pycocotools>=2.0.7
# NanoDet repo dependencies
pyyaml>=6.0
pytorch-lightning>=2.0
omegaconf>=2.3
termcolor>=2.4
# RF-DETR runtime
supervision>=0.21
pydantic>=2
peft>=0.10
# RF-DETR uses transformers.pytorch_utils.find_pruneable_heads_and_indices,
# removed in transformers 5.x. Pin to a 4.x release.
transformers>=4.40,<5
# Xception uses timm-backed backbones
timm>=0.9
# Loaded but optional; kept for parity with original training scripts
scikit-learn>=1.3
matplotlib>=3.8
tqdm>=4.66
